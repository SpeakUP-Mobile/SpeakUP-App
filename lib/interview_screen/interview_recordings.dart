import 'package:flutter/material.dart';

class InterviewRecordings extends StatelessWidget {
  const InterviewRecordings({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
